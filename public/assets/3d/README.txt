Place the required 3D models in this directory:

- lens.glb
- bar.glb
- cube.glb

The homepage hero falls back to procedural glass shapes if these files are missing,
but the imported models are used automatically when present at the paths above.
